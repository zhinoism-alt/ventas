{
  "name": "ventas-app",
  "version": "2.0.0",
  "description": "Sistema de control de ventas, inventario e IPTV — Supabase + Clerk + Vercel",
  "scripts": {
    "install:all": "cd frontend && npm install",
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build"
  }
}
